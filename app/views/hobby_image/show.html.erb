<div>
  <%= attachment_image_tag @hobby_image, :image %>
  <p>説明：<%= @hobby_image.caption %></p>
  <p>投稿日時：<%= @hobby_image.created_at.strftime('%Y/%m/%d') %></p>
  <% if @hobby_image.user == current_user %>
    <%= link_to "削除", hobby_image_path(@hobby_image), method: :delete %>
  <% end %>
</div>