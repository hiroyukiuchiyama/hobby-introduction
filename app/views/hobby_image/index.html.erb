<% @hobby_image.each do |hobby_image| %>
<div>
  <%= attachment_image_tag hobby_image, :image %>
  <p>投稿ユーザー画像：<%= image_tag('sample-author1.jpg') %></p>
  <p>説明：<%= hobby_image.caption %></p>
</div>
<% end %>